<!DOCTYPE html>
<html lang="en">
<head>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.6/css/bootstrap.min.css" integrity="sha384-1q8mTJOASx8j1Au+a5WDVnPi2lkFfwwEAa8hDDdjZlpLegxhjVME1fgjWPGmkzs7" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css" media="screen" title="no title" charset="utf-8">
  <meta charset="UTF-8">
  <title>Authors</title>
</head>
<body>
  <div class="container-fluid">
  <h1> See all authors!</h1>
   <table class="table">   
    <thead>
      <tr>
        <th>ID</th>
        <th>Author Name</th>
      </tr>
    </thead>
    <tbody> 
      <tr> 
        <% authors.forEach(function(author){ %>   
          <td><%= author.id %> </td>
          <td><%= author.name %> </td>
          <td>
            <form action="/authors/books/<%= author.id %>/new" method="GET">
              <input type="submit" value="Add Book"> 
            </form>

            <form action="/authors/books/<%= author.id %>" method="GET">
              <input type="submit" value="See All Books"> 
            </form>
            
            <form action="/authors/<%= author.id %>/edit?_method=GET" method="GET">
              <input type="submit" value="Edit Author">
            </form>
            
            <form action="/authors/<%= author.id %>?_method=DELETE" method="POST">
              <input type="submit" value="Delete Author"> 
            </form>
          </td>
        </tr>
      <% })%>
      </tbody>
    </table>
  </div>
</body>
</html>